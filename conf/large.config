process {
    executor = 'slurm'

    withLabel: process_multi {
        queue = params.large_queue
        cpus = params.large_cpus
        memory = params.large_memory
        time = params.large_time
    }
}
